# Each route describes how an incoming URL will be processed by Platform.sh.

"https://{default}/":
    type: upstream
    upstream: "notpro_website:http"

"https://www.{default}/":
    type: redirect
    to: "https://{default}/"